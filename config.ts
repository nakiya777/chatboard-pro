// Replace with your actual Firebase configuration
// For the app to function, you need to create a project in Firebase Console,
// enable Firestore and Authentication (Anonymous), and paste the config here.
export const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};

export const APP_ID = 'chat-board-pro-v2';

// Optional: For testing with a custom token if needed
export const INITIAL_AUTH_TOKEN = ''; 
